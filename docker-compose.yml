services:
    postgres:
      image: postgres:17
      container_name: norte-postgres
      environment:
        POSTGRES_DB: norte
        POSTGRES_USER: norte
        POSTGRES_PASSWORD: norte
      ports:
        - "5432:5432"
      volumes:
        - norte_postgres_data:/var/lib/postgresql/data

volumes:
  norte_postgres_data: